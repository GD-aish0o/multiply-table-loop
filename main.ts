// Write a for loop to print the multiplication table for the number 5.
// for experiment you can change the number of const table.
const table = 5;
const count = 12;

for (let i = 1; i <= count; i++) {
    console.log(`${table} X ${i} = ${table * i}`);
}
